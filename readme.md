```playsomething```
To use:

Jordan play music, prompt, genre.

Make sure mic is on and in capture mode (space in alsamixer). Install packages julius, ffmpeg, avconv-tools, flite, youtube-dl, vlc, curl, wget, to use. To run enter ./command.sh
